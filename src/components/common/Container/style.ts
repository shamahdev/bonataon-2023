import { css } from '@emotion/react'

const ContainerCss = {
  wrapper: css({
    display: 'flex',
    justifyContent: 'center',
    height: '100%',
    width: '100%',
  }),

  box: css({
    width: '100%',
    maxWidth: '800px',
    position: 'relative',
  }),
}

export default ContainerCss
